<!doctype html>
<html lang="en">
<head><title>My Document</title></head>
<body>
    <h1>List of People</h1>
    <ul>
    {% for out in output %}
        <li>{{ out.name|capitalize }}: {{ out.age }} year{{ out.age > 1 ? 's' : ''}} old</li>
    {% endfor %}
    </ul>
</body>
</html>
